# Prompt Engineering — Opt-In

**Extension**: Prompt Engineering Standards

## Opt-In Prompt

The following question is automatically included in the Requirements Analysis clarifying questions when this extension is loaded:

```markdown
## Question: Prompt Engineering Extension
Should prompt engineering standards be enforced for this project?

A) Yes — enforce all PROMPT rules as blocking constraints (recommended for projects building prompt libraries, AI-assisted workflows, or prompt templates)
B) Partial — enforce PROMPT rules only for prompt ID conventions and framework selection (suitable for projects with occasional prompt authoring needs)
C) No — skip all PROMPT rules (suitable for projects that do not involve prompt authoring or AI template creation)
X) Other (please describe after [Answer]: tag below)

[Answer]: 
```
